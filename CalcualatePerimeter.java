public class CalcualatePerimeter {
    public static void main(String[] args){
        int length = 25;
        int breadth = 30;

        double perimeter = 2 * (length + breadth);  // perimeter of a rectangle  = 2 * (length + breadth)
        System.out.println("The perimeter of rectangle is : " + perimeter);
    }
}
